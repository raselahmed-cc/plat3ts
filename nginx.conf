upstream nextjs {
    server app:3000;
}

server {
    listen 80;
    server_name plat3s.com www.plat3s.com;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/lib/letsencrypt;
    }

    # Redirect HTTP to HTTPS (uncomment after SSL is set up)
    # return 301 https://$host$request_uri;

    # Temporary: serve via HTTP until SSL is ready
    location / {
        proxy_pass http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Uncomment this block after running: docker compose run certbot
# server {
#     listen 443 ssl http2;
#     server_name plat3s.com www.plat3s.com;
#
#     ssl_certificate /etc/letsencrypt/live/plat3s.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/plat3s.com/privkey.pem;
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
#     gzip_min_length 1000;
#
#     location / {
#         proxy_pass http://nextjs;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
